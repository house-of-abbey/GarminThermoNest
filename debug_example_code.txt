    hidden var wl as WebLog;

        wl = new WebLog();


    hidden function logAuth(prefix as String) as Void {
        if (prefix.length() > 0) {
            prefix += " ";
        }
        wl.println(prefix + "oauthCode:         " + Properties.getValue("oauthCode"));
        wl.println(prefix + "accessToken:       " + Storage.getValue("accessToken"));
        wl.println(prefix + "accessTokenExpire: " + Storage.getValue("accessTokenExpire"));
        wl.println(prefix + "refreshToken:      " + Storage.getValue("refreshToken"));
    }

                wl.println("Authentication failed, deleting access and refresh tokens.");
                logAuth("onReceiveDeviceData()");


Logs: https://domain.name/path/test
Clear with: https://domain.name/path/test_clear.php
